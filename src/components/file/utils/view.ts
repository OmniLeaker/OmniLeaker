const view_z = (url: string, fileName?: any): void => {
  const link_z = document.createElement('a')

  link.setAttribute('href', url)
  link.setAttribute('target', '_blank')
  link.click()
}

export default view
